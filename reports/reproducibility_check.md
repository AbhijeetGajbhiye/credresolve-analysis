# Reproducibility check

This package was validated from a clean repository root using the supplied `raw/` files and the packaged dependency manifest.

## Checks passed

- `pip install -r pipeline/requirements.txt` is documented and the required runtime packages are specified.
- `python pipeline/run_pipeline.py` completes successfully.
- `python pipeline/qa_smoke_test.py` completes successfully.
- `python pipeline/analysis_reproduce.py` completes successfully.
- `python pipeline/supplementary_metrics.py` completes successfully through the main pipeline.
- `jupyter nbconvert --to notebook --execute notebook/collections_forensics.ipynb` completes with zero notebook errors from the repository root.
- The notebook also executes successfully when launched from inside `notebook/`.
- Golden payment IDs are unique.
- SQL and Python use the same canonical payment-deduplication order.
- Requested operational metric tables and statistical-investigation outputs are regenerated by the pipeline.

## Key outputs

- Audited SUCCESS cash: **₹93.53 Cr**.
- Jun -> Jul audited all-account cash: **+3.7%**.
- Jun -> Jul targeted-account recovery yield: **+11.8%**.
- Timing-aligned first-target 7-day payer movement: **+0.05 pp** (descriptive only).
- ₹10 Cr annual break-even hurdle: **~1.91 pp incremental payer lift**.
- Planning power at 10% holdout: **~3,464 total accounts** for a 1.91 pp MDE under the stated planning assumptions.

No machine-specific source path is required to reproduce the analysis.
